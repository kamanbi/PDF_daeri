# 화면 상세

총 5개. 온보딩·튜토리얼 화면은 만들지 않는다. 광고 배치는 `ads.md` 참조.

## UX 원칙 (2026-08-19 확정)

- **메뉴는 꼭 필요한 항목만.** 화면에 버튼·메뉴 항목을 추가하기 전 "없어도 되는가"를 먼저 묻는다.
- **최근 저장 파일은 홈에서 바로 보인다.** 목록을 뒤지지 않고 최상단에서 바로 이어볼 수 있어야
  한다(S1 최근 연 파일 섹션이 이 원칙의 구현체 — 2주차 완료).
- **"빠르다"는 인상을 준다.** 지연 로딩·즉시 반응·불필요한 대기 화면 최소화를 설계·구현
  전반의 우선순위로 둔다. 새 화면·기능을 만들 때마다 "이게 느리게 느껴지지 않는가"를 확인한다.
- **압축 결과는 항상 숫자(%)로 보여준다.** "줄었습니다"가 아니라 "4.2MB → 1.1MB (74% 감소)"
  형태로(`pipeline.md` 압축 절 참조).
- **압도적으로 간단한 UX.** 확인 창·중간 설정 화면·불필요한 단계를 만들지 않는다. 예시(2026-08-19
  확정, 압축 흐름): `PDF 열기(뷰어 진입) → [압축] 1탭 → 강도 선택(고화질/기본/최소) 1탭 →
  완료(감소율 숫자 표시) → [원본 삭제]/[공유]는 강제하지 않는 선택지`. 새 화면·기능을 설계할 때
  "이 단계를 없앨 수 있는가"를 먼저 묻는다.

---

## S1. 홈

**구성**
- 상단: 앱 이름, 설정 아이콘
- 섹션 1 — **최근 연 파일** (외부에서 연 PDF). 항목: 이름, 크기, 연 시각
- 섹션 2 — **내 문서** 2열 그리드. 썸네일 / 제목 / `12p · 3.4MB` / 날짜
- 하단 3진입점: **스캔** (주 버튼) / **PDF 열기** / **사진 → PDF**
- 최하단: 배너

**인터랙션**
- 문서 탭 → S4 뷰어
- 롱프레스 → 다중 선택: **합치기**(2개 이상), 삭제, 공유
- `⋮` → 이름 변경 / 나누기 / 압축 / 공유 / 삭제
- 최근 연 파일의 삭제는 **"목록에서 제거"** (원본을 지우지 않음)

**주의**
- 정렬은 최신순 고정. v1에서 정렬 옵션을 만들지 않는다
- 두 섹션을 한 그리드에 섞지 않는다

---

## S2. 스캔
- ML Kit 문서 스캐너 플로우 호출, 결과만 수신
- 자체 카메라·보정 UI를 만들지 않는다
- Play 서비스 실패 시 "사진 → PDF"로 유도. 앱이 죽지 않게 한다
- 이 화면에는 배너를 넣지 않는다

---

## S2-b. PDF 열기
- 시스템 파일 선택기(SAF) 호출
- 선택 → 앱 작업공간으로 복사 → S4 뷰어
- 외부 인텐트(`application/pdf` VIEW)로 진입한 경우도 동일 경로
- 암호 PDF는 비밀번호 입력, 손상 파일은 안내 후 종료

---

## S3. 페이지 편집

**진입**
- 외부 PDF에서 진입 시 최초 1회: **"복사본으로 편집합니다. 원본은 변경되지 않습니다."**

**구성**
- 3열 썸네일 그리드, 페이지 번호 표시
- 주요 액션(저장·페이지 추가)은 **상단 앱바**
- 하단: 완충 밴드 + 배너

**인터랙션**
- 드래그 → 순서 변경 (드래그 중 배너 슬라이드 아웃)
- 탭 → 선택 모드: 회전 / 삭제 / **선택 페이지로 새 문서**(나누기)
- 페이지 추가: 카메라 / 사진 (외부 PDF에 스캔 페이지 추가 = 혼합 문서)
- 저장 → 제목 입력 + 화질 선택 → **용량 검증 게이트 통과** → S4

**주의**
- 썸네일 지연 로딩. 저장 중 진행률 + 취소
- 저장 후 "새 파일로 저장됨" 안내

---

## S4. 뷰어
- 좌우 스와이프, 핀치 줌, 하단 썸네일 바
- 상단: 공유, 편집으로 이동
- 텍스트 선택·검색·주석 없음
- 배너와 제스처 영역 사이 완충 밴드

---

## S5. 설정
- **광고 제거** 구매 / 복원
- 기본 저장 화질
- 저장 공간 사용량 + 캐시 비우기 + 최근 파일 정리
- 오픈소스 라이선스
- 개인정보처리방침 (수집 없음, 서버 전송 없음 명시)
- 계정·로그인 항목 없음
