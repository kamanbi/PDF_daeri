# PDF 파이프라인

## 최상위 원칙

> **외부 PDF는 절대 래스터화하지 않는다. 페이지 객체를 그대로 복사한다.**

편집이란 **페이지 참조 목록을 재배열하는 것**이며, 원본 바이트를 건드리지 않는다.
100페이지 텍스트 PDF에서 한 장을 지워도 결과는 여전히 텍스트 PDF여야 하고, 용량은 줄어야 한다.

---

## 페이지 모델

```dart
sealed class PageRef {
  final int rotation; // 0/90/180/270
}

class ImagePageRef extends PageRef {   // 스캔·사진
  final String imagePath;
}

class PdfPageRef extends PageRef {     // 외부 PDF 또는 앱 내 PDF
  final String sourcePath;   // 앱 작업공간의 원본 복사본
  final int sourceIndex;     // 원본에서의 페이지 번호
}
```

하나의 문서에 두 종류가 **섞일 수 있다.** (외부 PDF에 스캔 페이지를 추가한 경우)

---

## 저장 동작

`PdfEngine.save(List<PageRef>, quality)`

| 페이지 종류 | 처리 | 손실 |
|---|---|---|
| `PdfPageRef` | 원본에서 **페이지 객체 import** | 없음 |
| `ImagePageRef` | JPEG 임베드 (quality 프리셋 적용) | 이미지 인코딩만 |
| 회전 | 페이지 **회전 속성만 변경** | 없음 (재인코딩 안 함) |

**금지 사항**
- PDF 페이지를 비트맵으로 렌더링해 다시 넣는 것 — 어떤 경우에도 금지
- 편집 저장 경로에서 이미지 재인코딩을 수행하는 것 (그건 압축 기능의 몫)

---

## 용량 증가 방지 (요구사항: 무손실 + 용량 증가 없음)

### 증가 경로 3가지와 대응

| 함정 | 증상 | 대응 |
|---|---|---|
| 증분 저장(incremental save) | 페이지를 지웠는데 용량 그대로/증가 | 항상 **전체 재작성(full rebuild)** 모드로 저장 |
| 고아 객체 | 페이지만 삭제되고 리소스 잔존 | 재작성 시 미참조 객체 제거 |
| 폰트·리소스 중복 | 합치기 결과가 원본 합보다 큼 | 저장 시 리소스 중복 제거. 라이브러리 미지원이면 사용자 경고 |

### 검증 게이트 (`size_guard.dart` · 자동 테스트로 고정)

저장 직전에 반드시 통과해야 한다.

| 작업 | 규칙 |
|---|---|
| 페이지 삭제 | `결과 ≤ 원본` |
| 순서 변경·회전만 | `결과 ≤ 원본 × 1.05` |
| 나누기 | `결과 ≤ 원본 × (선택페이지 / 전체페이지) × 1.2` |
| 합치기 | `결과 ≤ 원본 합계 × 1.05` |

**위반 시 저장을 중단하고 사용자에게 알린다.** 조용히 통과시키지 않는다.
이 규칙들은 유닛 테스트로 작성하며, 텍스트 PDF·스캔 PDF·혼합 문서 3종 픽스처로 매 빌드 검증한다.

---

## 압축 (편집 저장과 완전히 분리)

압축은 **의도적 손실**이다. 편집 저장 경로와 코드·UI 모두 분리한다.

1. **대상 판별** — 페이지가 이미지 위주(스캔본)인지 검사
   - 이미지 위주 → 재인코딩 진행
   - 텍스트·벡터 위주 → 실행하지 않고 "이 문서는 압축 효과가 거의 없습니다" 안내
2. **결과 검증** — 결과가 원본보다 크면 **자동으로 원본 유지** 후 알림
3. **결과 표시** — `4.2MB → 1.1MB (74% 감소)` 형태로 숫자를 보여준다

### 화질 프리셋 (이미지 페이지에만 적용)

| 프리셋 | 긴 변 최대 | JPEG 품질 | 용도 |
|---|---|---|---|
| high | 2480px | 85 | 도면·작은 글씨 |
| standard | 1754px | 75 | 기본값 |
| min | 1240px | 60 | 메일 첨부 |

**[2026-08-18 M-Q6 실측 갱신]** `pdf-core`가 `test/pdf/pdf_compressor_test.dart`(그룹 "프리셋
실측")에서 `QpdfCompressor.compress`(L2 이미지 재인코딩 + L1 qpdf 최적화, 실제 조합 경로)로
측정. 사용자 원본 PDF는 read-only 원칙상 압축 표 측정에 쓰지 않고(대신 §L1 단독 수치는
아래 참고), 10페이지 합성 "스캔형" JPEG 픽스처(2550×3300px ≈ 300dpi A4, 흰 배경 + 어두운
텍스트-밀도 노이즈 밴드, 품질 92 원본)를 원본 마스터로 사용해 3개 프리셋을 동일 입력으로
비교했다.

| 프리셋 | 원본(10p 합계) | 압축 후 | 감소율 | 페이지당 평균 |
|---|---|---|---|---|
| high | 40,205,116 B (38.3MB) | 18,688,889 B (17.8MB) | 53.5% | 약 1.87MB |
| standard | 40,205,116 B (38.3MB) | 7,552,818 B (7.2MB) | 81.2% | 약 0.75MB |
| min | 40,205,116 B (38.3MB) | 3,628,666 B (3.46MB) | 91.0% | 약 0.36MB |

RO(재오픈) 검증: 3개 프리셋 전부 결과 파일을 qpdf(`runInspect`)로 재오픈해 페이지 수(10)가
유지됨을 확인했다(`pdf_compressor_test.dart`).

**판정 기준("10페이지 스캔 문서가 `min`으로 2MB 이하") 재검토 필요**: 이 합성 픽스처는
페이지 전체 면적의 상당 부분에 조밀한 무작위 노이즈 밴드를 채워 넣어(실제 스캔본보다
AC 계수 밀도가 훨씬 높다) 의도적으로 압축이 어려운 최악에 가까운 사례를 만든다. `min`
결과(3.46MB)가 목표(2MB)를 초과한 것은 이 합성 데이터의 보수적 특성 때문일 가능성이
높다 — 실제 스캔본(대부분이 흰 여백이고 텍스트는 얇은 선)은 이보다 낮은 AC 계수 밀도를
가져 더 잘 압축될 것으로 예상되나, **실측 없이 이 추정을 확정하지 않는다**. 사용자의
실제 스캔 문서(2주차 이후 앱으로 직접 생성한 스캔 픽스처)로 재측정이 필요하다
[실측 필요 · 다음 라운드].

L1(무손실 최적화)만 단독 적용했을 때의 효과는 문서 성격에 따라 다르다 — 이미 qpdf로
저장된 문서(이 앱이 생성한 문서는 저장 시점에 이미 §5.2 공통 쓰기 옵션을 거친다)는 L1
재적용 효과가 거의 없고(`pdf_compressor_test.dart`의 "L1: 이미 최적인 문서" 케이스가
`keptOriginal` 분기를 커버), 아직 qpdf를 거치지 않은 외부 PDF에서 효과가 난다(정확한
비율은 `qpdf_isolate_test.dart`의 R5 재현 수치 0.92~0.93대와 같은 자릿수로 추정되나
페이지 삭제가 섞인 수치라 압축 전용 수치와 직접 비교하지 않는다 — 별도 실측 필요).

---

## 외부 PDF 임포트 흐름

```
시스템 파일 선택기(SAF) 또는 외부 인텐트
  → content:// URI 수신
  → 앱 작업공간으로 복사 (원본 미변경)
  → 열기/보기
  → [편집] 선택 시 "복사본으로 편집합니다" 안내
  → PageRef 목록 생성
  → 편집
  → 새 PDF로 저장 (원본 파일은 그대로)
```

- 원본 URI에 대한 지속 권한을 요청하지 않는다. 복사본만 다룬다
- 저장 후 "새 파일로 저장됨"을 명확히 알린다 (원본이 안 바뀐다는 오해 방지)

### 예외 처리 (반드시 만난다)

| 상황 | 동작 |
|---|---|
| 암호 걸린 PDF | 비밀번호 입력 요청. 실패 시 보기 불가 안내 (v1은 열기만, 암호 설정은 v1.1) |
| 손상 파일 | 앱이 죽지 않고 "열 수 없는 파일" 안내 |
| 500페이지+ / 100MB+ | 진행률 표시 + 취소 지원. 썸네일은 지연 로딩 |
| 특이 폰트·벡터 | 무손실 복사이므로 렌더링만 확인 |

---

## 파일명 규칙 (`file_name.dart` 단일 구현)

```
trim → NFC → 금지문자 치환(/ \ : * ? " < > |) → 100자 제한 → 빈 값이면 "문서_yyyyMMdd_HHmm"
```

- 합치기 결과: `<첫 문서 제목> 외 N건`
- 나누기 결과: `<원본 제목> (발췌)`
- 편집 저장 결과: `<원본 제목> (편집본)`
- 압축 결과: `<원본 제목> (압축)` (2026-08-19 확정, 외부 PDF L2 압축 포함)
- 동일 제목 존재 시 ` (2)` 부여

---

## 메모리·스레드

- 페이지를 한 번에 메모리에 올리지 않는다
- PDF 파싱·병합·압축은 **isolate에서 실행**. 비동기만으로는 UI가 멈춘다
- 50페이지 이상은 진행률 + 취소 지원
